function fixStepper(){const wrap=document.querySelector('.stepper'),input=wrap?.querySelector('input[type=number]');if(!wrap||!input||wrap.dataset.fixed)return;wrap.dataset.fixed='1';wrap.querySelectorAll('.stepper-btn').forEach(btn=>{btn.onclick=()=>{const delta=btn.textContent.includes('+')?1:-1;const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set;setter.call(input,String(Math.max(1,Number(input.value||1)+delta)));input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}))}})}
new MutationObserver(fixStepper).observe(document.body,{childList:true,subtree:true});setInterval(fixStepper,300);
